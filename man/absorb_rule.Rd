% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shared_helpers.R
\name{absorb_rule}
\alias{absorb_rule}
\title{Define an absorbing-state override rule}
\usage{
absorb_rule(cond, value, branch = c("any", "death", "dc"))
}
\arguments{
\item{cond}{An unquoted expression (evaluated row-wise in the long-format
data) that identifies rows to which the rule applies. Variables from the
dataset and the special symbols \code{t} (current time) and \code{branch}
(\code{"death"} or \code{"dc"}) are in scope.}

\item{value}{An unquoted expression giving the outcome value to assign when
\code{cond} is \code{TRUE}. May reference data variables or constants. Scaled and
clipped automatically via \code{scale_info}.}

\item{branch}{Which exit branch the rule applies to: \code{"any"} (both death
and discharge, the default), \code{"death"}, or \code{"dc"} (discharge).}
}
\value{
A named list with elements \code{branch}, \code{cond}, and \code{value} (the latter
two as unevaluated expressions via \code{\link[base:substitute]{base::substitute()}}).
}
\description{
Constructs a rule that overrides Q-model predictions for subjects in a
specific absorbing state. Pass one or more rules to the \code{absorb} argument
of \code{\link[=sdr]{sdr()}} or \code{\link[=itmle]{itmle()}}.
}
\examples{
# Force outcome to 0 for patients who die at any time
absorb_rule(alive == 0, value = 0, branch = "death")

# Force outcome to 1 for discharged patients when t >= 5
absorb_rule(in_state == 0 & t >= 5, value = 1, branch = "dc")

}
