% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_data.R
\name{sim_multi}
\alias{sim_multi}
\title{Simulated ICU panel with multiple treatments (any mix of binary + continuous)}
\usage{
sim_multi(
  n = 2000L,
  tmax = 5L,
  seed = 1L,
  n_binary = 1L,
  n_continuous = 1L,
  apply_policy = FALSE
)
}
\arguments{
\item{n}{Number of subjects.}

\item{tmax}{Maximum follow-up.}

\item{seed}{Integer seed.}

\item{n_binary}{Number of binary treatments (>= 0).}

\item{n_continuous}{Number of continuous treatments (>= 0).}

\item{apply_policy}{Logical. When \code{TRUE}, forces every binary treatment to
\code{1} for subjects with \code{L1 > 1.0} and adds \code{+0.2} (capped at \code{2.0}) to
every continuous treatment at each time-step -- used internally to
generate Monte-Carlo truth for accuracy tests. Leave at \code{FALSE} for
regular observed data.}
}
\value{
A \code{data.frame} with columns
\code{id}, \code{time}, \code{age}, \code{sex}, \code{L1}, \verb{A_b1..A_b\{n_binary\}},
\verb{A_c1..A_c\{n_continuous\}}, \code{alive}, \code{in_state}, \code{Y}.
}
\description{
Longitudinal ICU-style trajectories with an arbitrary number of binary
and/or continuous treatments, competing exit events, and a binary outcome.
Generalises \code{\link[=sim_bin]{sim_bin()}} / \code{\link[=sim_cont]{sim_cont()}} to multi-treatment MTPs.
}
\details{
Binary treatments are named \verb{A_b1, A_b2, ...} (first \code{n_binary} columns);
continuous treatments are named \verb{A_c1, A_c2, ...}. Each treatment enters
the exit-event and outcome models with a coefficient scaled by
\code{1 / sqrt(k)} where \code{k} is the treatment index within its type, so adding
more treatments does not blow up effect sizes.
}
\seealso{
\code{\link[=sim_bin]{sim_bin()}}, \code{\link[=sim_cont]{sim_cont()}}, \code{\link[=sdr]{sdr()}}, \code{\link[=itmle]{itmle()}}
}
