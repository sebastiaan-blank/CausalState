% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sl_itmle.R
\name{sl_itmle}
\alias{sl_itmle}
\alias{SL.tgt.empty}
\alias{SL.tgt.intercept}
\alias{SL.tgt.glm}
\alias{SL.tgt.glmnet}
\alias{SL.tgt.xgboost}
\alias{SL.tmle_empty}
\alias{SL.tmle_intercept}
\alias{SL.tmle_glm}
\alias{SL.tmle_glmnet_ridge}
\alias{SL.tmle_glmnet_enet}
\alias{SL.tmle_glmnet_lasso}
\alias{SL.tmle_xgb_d1}
\alias{SL.tmle_xgb_d3}
\alias{SL.tmle_xgb_d6}
\title{Custom SuperLearner wrappers for the iTMLE targeting step}
\usage{
SL.tgt.empty(Y, X, newX, family, obsWeights, id, bounds = 1e-05, ...)
}
\arguments{
\item{Y}{Numeric outcome vector (on \verb{[0, 1]} after scaling).}

\item{X}{\code{data.frame} of covariates, \strong{must} include a column named
\code{._sl_offset} containing the logit of the current Q estimate.}

\item{newX}{\code{data.frame} for prediction, same structure as \code{X}.}

\item{family}{Passed by SuperLearner; should be \code{binomial()}.}

\item{obsWeights}{Numeric vector of observation weights (the iTMLE clever
covariate / density ratio product).}

\item{id}{Subject identifiers (passed by SuperLearner, not used directly).}

\item{bounds}{Numeric. Clipping bound for predictions. Default \code{1e-5}.}

\item{...}{Additional arguments (ignored).}
}
\value{
A list with elements \code{pred} (numeric predictions on the probability
scale) and \code{fit} (a fitted object with a \code{predict} method).
}
\description{
A family of SuperLearner-compatible learner functions designed for the
iTMLE targeting step. They differ from standard wrappers in one critical
way: the logit offset is passed as a \strong{column} in \code{X} (named
\code{._sl_offset}) rather than via the \code{offset} argument. This is necessary
because SuperLearner's internal cross-validation subsetting drops the
\code{offset} vector, whereas a column in \code{X} is correctly subset.

Each learner:
\enumerate{
\item Extracts \code{._sl_offset} from \code{X}/\code{newX} via \code{extract_offset()}.
\item Fits a fluctuation model with that offset.
\item Returns predictions on the probability scale, clipped to \verb{[bounds, 1-bounds]}.
}

Learners available:
\describe{
\item{\code{SL.tgt.empty}}{No fluctuation: returns \code{expit(offset)} unchanged.
Acts as the "no update" option.}
\item{\code{SL.tgt.intercept}}{One-parameter intercept fluctuation (standard
TMLE update).}
\item{\code{SL.tgt.glm}}{Main-terms logistic GLM fluctuation with offset.}
\item{\code{SL.tgt.glmnet}}{Penalised logistic regression (elastic net) with
offset via \code{glmnet::cv.glmnet()}.}
\item{\code{SL.tgt.xgboost}}{Gradient boosted trees with offset via
\code{base_margin}. CUDA-capable.}
}

Pre-configured variants (\verb{SL.tmle_*}) expose specific hyperparameter
choices and are the recommended building blocks for \code{sl_tmle}:
\describe{
\item{\code{SL.tmle_empty}}{No fluctuation (pass-through).}
\item{\code{SL.tmle_intercept}}{One-parameter intercept (standard TMLE update).}
\item{\code{SL.tmle_glm}}{Main-terms logistic GLM.}
\item{\code{SL.tmle_glmnet_ridge}}{Ridge regression (alpha = 0).}
\item{\code{SL.tmle_glmnet_enet}}{Elastic net (alpha = 0.5).}
\item{\code{SL.tmle_glmnet_lasso}}{Lasso (alpha = 1).}
\item{\code{SL.tmle_xgb_d1}}{XGBoost depth 1, CPU, 50 rounds.}
\item{\code{SL.tmle_xgb_d3}}{XGBoost depth 3, CPU, 50 rounds.}
\item{\code{SL.tmle_xgb_d6}}{XGBoost depth 6, CPU, 50 rounds.}
}

The default \code{sl_tmle} vector follows Luedtke et al. (2017) Algorithm 4:
\code{empty}, \code{intercept}, \code{glm}, and the three XGBoost depths.  The glmnet
variants are available but must be specified explicitly via \code{sl_tmle}.
}
\examples{
# Default targeting stack (Luedtke et al. Algorithm 4)
sl_tmle

# Lightweight stack for examples and testing (no XGBoost required)
sl_tmle_light <- c("SL.tmle_empty", "SL.tmle_intercept", "SL.tmle_glm")

# Add glmnet variants explicitly when desired
sl_tmle_penalised <- c(
  "SL.tmle_empty", "SL.tmle_intercept", "SL.tmle_glm",
  "SL.tmle_glmnet_ridge", "SL.tmle_glmnet_enet",
  "SL.tmle_xgb_d1", "SL.tmle_xgb_d3", "SL.tmle_xgb_d6"
)

}
\seealso{
\code{\link[=itmle]{itmle()}}
}
